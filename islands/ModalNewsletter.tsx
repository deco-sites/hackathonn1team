import Component from "$store/components/ui/ModalNewsletter.tsx";
import type { Props } from "$store/components/ui/ModalNewsletter.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
